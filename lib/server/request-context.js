module.exports = ({
  headers,
  cookies,
  hostname,
  ip,
  originalUrl,
  params,
  path,
  protocol,
  query,
  route,
  subdomains,
  xhr,
}) => ({
  headers,
  cookies,
  hostname,
  ip,
  originalUrl,
  params,
  path,
  protocol,
  query,
  route,
  subdomains,
  xhr,
});
